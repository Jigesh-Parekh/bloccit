
<h1>New Ad </h1>

 <div class="row">
   <div class="col-md-4">
     <p>Guidelines for posts</p>
     <ul>
       <li>Grab all da dollars</li>
       <li>Mimic BuzzFeeds Clickbait garbage</li>
       <li>Throw all of your ethics and morals out the window.</li>
     </ul>
   </div>
   <div class="col-md-8">
     <%= form_for @advertisements do |a| %>
       <div class="form-group">
         <%= a.label :title %>
         <%= a.text_field :title, class: 'form-control', placeholder: "Enter Ad Title" %>
       </div>
       <div class="form-group">
         <%= a.label :price %>
         <%= a.text_field :price, class: 'form-control', placeholder: "Enter Price" %>
       </div>
       <div class="form-group">
         <%= a.label :body %>
         <%= a.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter Ad information" %>
       </div>
       <div class="form-group">
         <%= a.submit "Save", class: 'btn btn-success' %>
       </div>
     <% end %>
   </div>
 </div>